<%= form_for(@billable_hour) do |f| %>
  <% if @billable_hour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@billable_hour.errors.count, "error") %> prohibited this billable_hour from being saved:</h2>

      <ul>
      <% @billable_hour.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :start %><br>
    <%= f.datetime_select :start %>
  </div>
  <div class="field">
    <%= f.label :end %><br>
    <%= f.datetime_select :end %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.text_field :project_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
